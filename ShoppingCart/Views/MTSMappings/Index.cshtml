@model IEnumerable<ShoppingCart.Database.MTSMapping>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShowTiming.ShowId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Theatre.TheatreName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShowTiming.ShowId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Theatre.TheatreName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MTSMappingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MTSMappingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MTSMappingId })
        </td>
    </tr>
}

</table>
